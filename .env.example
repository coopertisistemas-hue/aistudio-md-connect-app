# =============================================================================
# MD Connect — Environment Variables
# Copy to .env.local for local development (never commit .env.local)
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED — App will not start without these
# -----------------------------------------------------------------------------

# Supabase project URL (Supabase Dashboard → Settings → API → Project URL)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anonymous/public key (Supabase Dashboard → Settings → API → anon public)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# -----------------------------------------------------------------------------
# OPTIONAL — Feature flags and runtime configuration
# -----------------------------------------------------------------------------

# Application environment identifier (development | staging | production)
VITE_APP_ENV=development

# Application version string — shown in error reports
VITE_APP_VERSION=1.0.0

# Public base URL for OG/SEO meta tags (e.g. https://app.mdconnect.com.br)
VITE_PUBLIC_BASE_URL=http://localhost:5173

# Base URL for server-side error reporting endpoint (leave empty to disable)
VITE_API_BASE_URL=

# -----------------------------------------------------------------------------
# OPTIONAL — Analytics (leave empty/false to disable)
# -----------------------------------------------------------------------------

# Google Analytics 4 Measurement ID (e.g. G-XXXXXXXXXX)
VITE_GA_MEASUREMENT_ID=

# Enable/disable Google Analytics frontend tracking (true | false)
VITE_ANALYTICS_ENABLED=false

# Enable/disable backend event tracking via Supabase Edge Function (true | false)
VITE_ENABLE_BACKEND_ANALYTICS=false

# -----------------------------------------------------------------------------
# OPTIONAL — Error reporting
# -----------------------------------------------------------------------------

# Enable/disable client-side error reports to backend (true | false)
VITE_ERROR_REPORTING_ENABLED=false

# =============================================================================
# SUPABASE EDGE FUNCTIONS (Deno) — NOT set in .env.local
# These are auto-injected by Supabase CLI (local) and Supabase Platform (cloud)
# =============================================================================
# SUPABASE_URL              — auto-injected
# SUPABASE_ANON_KEY         — auto-injected
# SUPABASE_SERVICE_ROLE_KEY — auto-injected (privileged functions only)
#
# OPENAI_API_KEY  — MUST be set in Supabase project secrets for AI functions:
#   supabase secrets set OPENAI_API_KEY=sk-...
#   (affects: devotionals-generate-cover, generate-verse-commentary, generate-book-context)
# =============================================================================
